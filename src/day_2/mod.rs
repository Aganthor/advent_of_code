pub mod day_2_solution {
    use std::fs::File;
    use std::io::{BufRead, BufReader};

        //
    //Day 1 advent of code solver!
    //
    pub fn solve_day_2() -> u32 {
        let file = File::open("./input.txt").expect("Failed to read input file.");
        let file = BufReader::new(file);

    }
}